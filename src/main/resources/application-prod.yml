spring:
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: ${JWT_ISSUER_URI}
          audiences: ${JWT_AUDIENCE:clih-api}

app:
  security:
    mode: jwt
  cors:
    allowed-origins: ${CORS_ALLOWED_ORIGINS}
